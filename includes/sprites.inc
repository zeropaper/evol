<?php
function evol_sprite($options) {
  if (empty($options['preset'])
    || empty($options['width'])
    || empty($options['height'])
    || !is_array($options['files'])
    || empty($options['files'])) {
    return FALSE;
  }
  
  
  
}